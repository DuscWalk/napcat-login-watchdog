# Add this line with `(crontab -l 2>/dev/null; cat deploy/cron/napcat-login-watchdog) | crontab -`.
* * * * * cd /opt/napcat-login-watchdog && set -a && . /opt/napcat-login-watchdog/.env && set +a && /opt/napcat-login-watchdog/.venv/bin/napcat-login-watchdog run
